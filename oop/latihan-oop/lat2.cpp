#include <bits/stdc++.h>
using namespace std;

class Class {
    public:
    Class(int a, int b) {
        cout << a + b << endl;
    };
};

int main() {
    int a, b;
    cin >> a >> b;
    Class kelas(a, b);
    
}